# Development placeholders only. Never commit real secrets.

POSTGRES_IMAGE=postgres:16-alpine
REDIS_IMAGE=redis:7-alpine
NEXTCLOUD_IMAGE=nextcloud:stable
OPENFGA_IMAGE=openfga/openfga:latest

LISA_DB_PASSWORD=change-me
NEXTCLOUD_DB_PASSWORD=change-me

# Real 1Password credentials/service-account tokens must be injected at runtime.
# OP_SERVICE_ACCOUNT_TOKEN=

# Runtime endpoints will be added when the OpenClaw adapters are implemented.
# OPENCLAW_PRIVATE_URL=
# OPENCLAW_SANDBOX_URL=
