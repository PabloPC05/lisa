# Laboratory backend ONLY. The UI demo needs no environment variables.
# Copy to a private file outside the repository. Values intentionally empty.
# Select reviewed, compatible image versions/digests; no floating latest/stable defaults.
POSTGRES_IMAGE=
REDIS_IMAGE=
NEXTCLOUD_IMAGE=
OPENFGA_IMAGE=
LISA_DB_PASSWORD=
NEXTCLOUD_DB_PASSWORD=

# Do not put personal 1Password, model or OpenClaw tokens here.
# A future credential broker must obtain its runtime secrets separately.
# No private integration is currently implemented.
